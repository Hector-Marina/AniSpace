% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/areaUD.R
\name{areaUD}
\alias{areaUD}
\title{Estimate individual spatial utilisation distributions and home ranges}
\usage{
areaUD(AniObj, method = "HR", res = c(100, 100), verbose = TRUE)
}
\arguments{
\item{AniObj}{An object of class \code{AniSpace} containing the individuals' spatio-temporal position information.}

\item{method}{A character value specifying the spatial-utilisation approach. Available options are "grid", "poly", and "HR".
\code{grid} estimates a grid-based utilisation distribution for each individual.
\code{poly} estimates polygon-based utilisation areas from the observed positions.
\code{HR} estimates individual home ranges using kernel utilisation distributions (Default: \emph{method="HR"}).}

\item{res}{A positive numeric vector of length two specifying the spatial resolution in the x and y directions, respectively.
Values must use the same spatial units as the coordinates stored in \code{AniObj}. (\emph{Default: c(100,100)}).}

\item{verbose}{A logical variable indicating whether the function will print relevant information (TRUE) or not (FALSE) (\emph{Default: TRUE}).}
}
\value{
The modified \code{AniSpace} object with the estimated utilisation distributions or home-range results stored in the \code{UD} slot.
}
\description{
\code{areaUD} estimates the spatial utilisation of each individual from
the position information stored in an \code{AniSpace} object. Depending on the
selected method, the function returns a grid-based utilisation distribution,
a polygon-based utilisation area, or a kernel-based home-range estimate.
}
\examples{
# Filter individuals
df.ID.filt=filterAniSpace(AniObj,NIDs=c(3:7))

# Estimate kernel-based home ranges
df.HR  =areaUD(df.ID.filt, method = "HR")

# Estimate grid-based utilisation distributions
df.UD  =areaUD(df.ID.filt, method = "grid")

# Estimate polygon-based utilisation areas
df.poly=areaUD(df.ID.filt, method = "poly")

}
