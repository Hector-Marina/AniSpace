% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fitERGM.R
\name{fitERGM}
\alias{fitERGM}
\title{Fit dyadic interaction models to AniSpace data}
\usage{
fitERGM(AniObj, method = "time", int.thr = 600, fix.eff = NULL, verbose = TRUE)
}
\arguments{
\item{AniObj}{An \code{AniSpace} object containing individual information and
spatial-interaction results.}

\item{method}{A character value specifying the interaction measure used as
the response. Available options are \code{"time"}, and \code{"nint"}, and \code{"mean"}.
\code{"time"} uses the duration for which each pair of individuals was separated
by less than the distance threshold used to estimate spatial interactions.
\code{"nint"} uses the number of proximity-interaction events between each pair.
\code{"mean"} fits the corresponding average of duration of interaction per instance.
(\emph{Default: "time"}).}

\item{int.thr}{Either \code{NULL} or a positive numeric value specifying the
threshold used to dichotomise the selected interaction measure. When \code{NULL},
interaction values are transformed as \code{log(1+x)} and then standardised to
have mean 0 and standard deviation 1 before fitting a linear model. When
supplied, values equal to or greater than \code{int.thr} are coded as an
interaction and a binomial logistic model is fitted (\emph{Default: 600}).}

\item{fix.eff}{Either \code{NULL} or a character vector containing the names of
individual-level variables stored in \code{AniObj@Info} to include as fixed
effects. For each selected variable, pairwise predictors are constructed for
every dyad and included in the fitted model. When \code{NULL}, pairwise predictors
are generated from all variables available in \code{AniObj@Info}
(\emph{Default: NULL}).}

\item{verbose}{A logical value indicating whether informative messages are
printed (\emph{Default: TRUE}).}
}
\value{
A list containing the pairwise model data in \code{data} and the fitted
model object or objects in \code{model}.
}
\description{
\code{fitERGM} constructs pairwise data from the individual attributes
stored in \code{AniObj@Info} and the spatial-interaction information stored in
\code{AniObj@SI}. Depending on \code{int.thr}, the selected interaction measure is
analysed either as a continuous response or converted into a binary network.
}
\examples{
# Filter individuals
df.ID.filt=filterAniSpace(AniObj,NIDs=c(1:7))

# Estimate spatial interactions required for the dyadic models
df.SI=spatialInt(df.ID.filt,method="all",verbose=FALSE)

# Estimate utilisation-distribution similarities
df.UD=areaUD(df.SI,method="grid",verbose=FALSE)
df.sim=UDsim(df.UD,verbose=FALSE)

# Fit a linear model to the normalised interaction values
ergm.linear=fitERGM(df.sim,method="time",int.thr=NULL,
                    fix.eff=c("Parity","DIM"),verbose=FALSE)
head(ergm.linear$data)
summary(ergm.linear$model)

# Fit a logistic model to dichotomised interaction values
df.sim.filt=filterAniSpace(df.sim,NIDs=c(3:7))
ergm.binary=fitERGM(df.sim.filt,method="nint",int.thr=10,
                  fix.eff=c("DIM"),verbose=FALSE)

head(ergm.binary$data)
summary(ergm.binary$model)

}
