% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats.Mov.R
\name{stats.Mov}
\alias{stats.Mov}
\title{Analyse individual movement and area-level behavioural clustering}
\usage{
stats.Mov(AniObj, k.means = FALSE, k.mov = NULL, k.max = 10, verbose = TRUE)
}
\arguments{
\item{AniObj}{An object of class \code{AniSpace} containing the individuals' spatio-temporal position information.}

\item{k.means}{Logical. If \code{TRUE}, a k-means cluster analysis is performed using step distance, speed, and turning angle (\emph{Default: FALSE}). The Kmeans cluster analysis uses the step distance, speed and turning angle information from all individuals in the \code{AniObj} object to assign \code{k.mov} behaviours to the different areas detailed in the object.}

\item{k.mov}{A positive integer specifying the number of movement-behaviour clusters. If \code{NULL}, the number of clusters is estimated by comparing the within-cluster sum of squares for values from 1 to \code{k.max} (\emph{Default: NULL}).}

\item{k.max}{A positive integer specifying the maximum number of clusters considered when estimating \code{k.mov} (\emph{Default: 10}).}

\item{verbose}{A logical variable specifying whether to print informative messages (\emph{Default: TRUE}).}
}
\value{
A list containing:
\itemize{
** \item \code{movement}: a data frame with position coordinates, time intervals, step distances, speeds, turning angles, and cluster assignments when clustering is performed.**
** \item \code{cluster}: a data frame summarizing cluster counts within each defined area, or NULL when clustering is not performed or no areas are defined.**
}
}
\description{
\code{stats.Mov} calculates step distances, speeds, and turning angles for each individual. When requested, it applies k-means clustering to movement observations and summarizes the occurrence of each movement-behaviour cluster within the areas defined in \code{AniObj}.
}
\examples{
# Calculate individual movement information
df.mov=stats.Mov(AniObj,verbose=FALSE)
head(df.mov$movement)

# Classify movement into two behavioural clusters
set.seed(123)
df.mov2=stats.Mov(AniObj,k.means=TRUE,k.mov=2,verbose=FALSE)

# Inspect movement information and assigned clusters
head(df.mov2$movement)

# Inspect the occurrence of each cluster within the defined areas
head(df.mov2$cluster)

}
\keyword{individual}
\keyword{information}
\keyword{movement}
