% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotUD.R
\name{plotUD}
\alias{plotUD}
\title{Plot individual spatial utilisation distributions and home ranges}
\usage{
plotUD(
  AniObj,
  NIDs = 1,
  IDs = NULL,
  HR.level = 95,
  plot.Pos = FALSE,
  grid.col = (grDevices::colorRampPalette(c("gold", "orange", "darkred")))(50),
  verbose = TRUE,
  ...
)
}
\arguments{
\item{AniObj}{An object of class \code{AniSpace} containing spatial-utilisation
results in the \code{UD} slot.}

\item{NIDs}{A numeric vector indicating the numerical identifiers of the
individuals to plot (\emph{Default: 1}).}

\item{IDs}{A character vector indicating the identifiers of the individuals
to plot (\emph{Default: NULL}).}

\item{HR.level}{A numeric value indicating the home-range percentage contour
plotted when the stored method is \code{"HR"} (\emph{Default: HR.level=95}).}

\item{plot.Pos}{A logical variable indicating whether individual positions
should be added to the plot (\emph{Default: FALSE}).}

\item{grid.col}{A vector of colours used for grid-based utilisation
distributions.}

\item{verbose}{A logical variable indicating whether the function will print
relevant information (TRUE) or not (FALSE) (\emph{Default: TRUE}).}

\item{...}{Additional graphical arguments passed to \code{plot()}.}
}
\value{
Plot AniSpace utilisation distribution information
}
\description{
\code{plotUD} plots individual spatial-utilisation distributions stored
in the \code{UD} slot of an \code{AniSpace} object. The spatial areas are plotted
first, followed by the grid-based utilisation distribution, convex polygon,
or kernel home-range contour estimated by \code{areaUD}.
}
\examples{
# Filter individuals
df.ID.filt=filterAniSpace(AniObj,NIDs=3)

# Plot a grid-based utilisation distribution
df.UD=areaUD(df.ID.filt,method="grid")
plotUD(df.UD,NIDs=1)

# Plot a polygon-based utilisation area
df.poly=areaUD(df.ID.filt,method="poly")
plotUD(df.poly, NIDs=1, plot.Pos=TRUE)

# Plot a 95 percent kernel-based home range
df.HR=areaUD(df.ID.filt,method="HR")
plotUD(df.HR,NIDs=1,HR.level=95)

}
