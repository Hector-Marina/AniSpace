% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/UDsim.R
\name{UDsim}
\alias{UDsim}
\title{Estimate pairwise utilisation-distribution similarity}
\usage{
UDsim(AniObj, sim.method = "continuous", HR.level = 95, verbose = TRUE)
}
\arguments{
\item{AniObj}{An object of class \code{AniSpace} containing utilisation
distributions previously estimated with \code{areaUD}.}

\item{sim.method}{A character value specifying whether similarity is based
on spatial occupancy (\code{"binary"}) or the magnitude of utilisation
(\code{"continuous"}). The calculation depends on the stored utilisation method:
\itemize{
\item For \code{"grid"}, \code{"binary"} estimates the proportion of the focal
individual's occupied cells also occupied by the other individual.
\code{"continuous"} estimates the proportion of the focal individual's
locations shared with the other individual.
\item For \code{"poly"}, \code{"binary"} estimates the proportion of the focal polygon's
grid cells also contained in the other polygon. \code{"continuous"} estimates
the proportion of the focal polygon's exact area overlapped by the other
polygon.
\item For \code{"HR"}, \code{"binary"} estimates the proportion of the focal individual's
cells within the \code{HR.level} contour also contained within the other
individual's contour (Hurlbert's statistic). \code{"continuous"} estimates the
conditional Bhattacharyya affinity between both utilisation distributions
after restricting and normalising them within their \code{HR.level} contours.
}

Grid, polygon, and binary home-range similarities are directional, whereas
continuous home-range similarity is symmetric (\emph{Default: "continuous"}).}

\item{HR.level}{A numeric value between 0 and 100 specifying the probability
contour used for binary and continuous kernel home-range comparisons. This
argument is only used when the stored utilisation method is \code{"HR"}
(\emph{Default: 95}).}

\item{verbose}{A logical value indicating whether progress messages are
printed (\emph{Default: TRUE}).}
}
\value{
The modified \code{AniSpace} object with the pairwise similarity matrix
stored in the \code{UDsim} slot. For directional methods, element \verb{[i,j]}
represents the proportion of individual \code{i}'s utilisation shared with
individual \code{j}.
}
\description{
\code{UDsim} estimates pairwise spatial-utilisation similarities between
individuals in an \code{AniSpace} object. The calculation depends on the
utilisation-distribution method previously applied with \code{areaUD}:
grid-cell use, polygon overlap, or kernel home-range overlap. Similarities
range from 0, indicating no shared utilisation, to 1, indicating complete
similarity of the focal individual's utilisation.
}
\examples{
# Filter individuals
df.ID.filt=filterAniSpace(AniObj,NIDs=c(3:7))

# Estimate pairwise grid-based utilisation-distribution similarities
df.UD=areaUD(df.ID.filt,method="grid")
df.UD.sim=UDsim(df.UD)

# Estimate pairwise polygon-based utilisation-distribution similarities
df.poly=areaUD(df.ID.filt,method="poly")
df.poly.sim=UDsim(df.poly)

# Estimate pairwise kernel-based utilisation-distribution similarities
df.HR=areaUD(df.ID.filt,method="HR")
df.HR.sim=UDsim(df.HR)

}
